# CHANGELOG – BESS_MAP_DE

- 2026-08-09 — Installed Python 3.12 locally (winget) — needed for local pipeline prototyping/iteration; the weekly GitHub Action itself doesn't require it (Actions runners ship Python).
- 2026-08-09 — Validated MaStR Gesamtdatenexport access pattern: single ~3.06GB daily ZIP, no per-object API; built `scripts/rangefile.py` (HTTP range-request seekable file + single-request member fetch) to pull only the Stromspeicher-related members (~650MB) instead of the full archive — `remotezip` package failed against this server for unknown reasons, replaced with a small dependency-free implementation.
- 2026-08-09 — Validated join/filter design against real data: `EinheitenStromSpeicher` (unit-level, power/location/status, sharded into 28 files) joined to `AnlagenStromSpeicher` (Anlage-level, usable capacity in kWh) via `SpeMastrNummer` == `MaStRNummer`; status/tech codes resolved via `Katalogwerte.xml`. Filter `Batterietechnologie` present + `Bruttoleistung` >= 100 kW gives 98% coordinate coverage and correctly excludes residential batteries / pumped-hydro.
- 2026-08-09 — Ran full pipeline: 2,668 BESS units matched filter, 2,631 mappable. Wrote `data/bess_de.csv` and `data/bess_de.geojson`.
- 2026-08-09 — Built `index.html` (Leaflet map, OSM tiles, status-colored/power-sized markers, popup with power/capacity/dates) and `.github/workflows/update.yml` (weekly Monday 05:00 UTC pipeline run + auto-commit + Pages deploy).
- 2026-08-09 — Removed exploratory one-off scripts (check_anlagen.py, check_bess_coords.py, check_coords.py, check_katalog.py, fetch_sample.py, inspect_shard.py) after consolidating their reusable logic into rangefile.py — kept repo to only what the pipeline needs.
- 2026-08-09 — Initial commit pushed to github.com/20Luca20/BESS_MAP_DE (main branch).
- 2026-08-09 — User enabled GitHub Pages (Settings > Pages > Source: GitHub Actions) and manually ran the workflow — first data commit (5601d50) and Pages deploy confirmed.
- 2026-08-09 — Redesigned index.html per user request: search (name/Ort/Landkreis/MaStR-Nr), category filter (Utility-Scale >=1MW / Klein-C&I <1MW / Alle), utility-scale size-bucket filter (<10/10-25/25-50/50-100/100-200/>200 MW — cleaner round numbers proposed in place of the user's initial sketch, flagged not asked), Bundesland dropdown filter with graphical highlight, cumulative MW/MWh time-series charts (Realisiert vs. inkl. Pipeline) that respect all active filters, stats panel, dashboard sidebar layout, CartoDB Positron basemap, Germany national outline + darkened-outside-Germany mask for geographic emphasis.
- 2026-08-09 — Vendored German national + Bundesländer boundary GeoJSON (data/geo/) from github.com/isellsoap/deutschlandGeoJSON (Unlicense/public domain, low-res 4_niedrig variant) — needed for the Germany/Bundesland highlight features; Bundesland `properties.name` values confirmed to exact-match MaStR's resolved Bundesland field, no mapping table needed.
- 2026-08-09 — Fixed workflow trigger gap: original workflow only ran on schedule/workflow_dispatch, so frontend-only pushes never redeployed Pages. Split into `push`-triggered deploy (skips the ~650MB data pipeline) vs. schedule/workflow_dispatch-triggered full data refresh, using `if: github.event_name != 'push'` / `if: ${{ !cancelled() && !failure() }}` job conditions.
- 2026-08-09 — Fixed a bug caught in self-review before pushing: `interactive: false` was nested inside the `style` object for two L.geoJSON layers (Germany outline, Bundesländer boundaries) instead of being a sibling top-level option — Leaflet only reads `interactive` at the top level, so as written those layers would have stayed clickable and could intercept clicks meant for BESS markers near state borders.
- 2026-08-09 — Remote had diverged (user's manual workflow run committed a data update) — rebased local frontend commit on top before pushing rather than force-pushing, no conflicts (touched disjoint files).
- 2026-08-10 — Major redesign per user request: 3-pane resizable layout (sidebar | map | graph, draggable dividers), merged the two stacked MW/MWh charts into one chart with a segmented MW/MWh toggle, added a top Kennzahlen/stats bar (always visible regardless of pane), Apple system-color palette (systemGreen/Blue/Orange/Red/Gray) applied to markers/legend/chart, frosted-glass topbar, full DE/EN i18n (UI strings + Bundesland names + status labels) with a top-right toggle.
- 2026-08-10 — Investigated user's "how can In Betrieb be in the future" question: confirmed via CSV query it's NOT a data bug (zero "In Betrieb" records have a future Inbetriebnahmedatum) — it was a chart-design issue, the "Realisiert" line was drawn flat across future x-axis years shared with the "inkl. Pipeline" projection. Fixed by capping the Realisiert series to stop (null/gap) after the current year, and added a bilingual footnote under the chart explaining both series and the pre-2010 exclusion.
- 2026-08-10 — Self-review before push caught a latent responsive-layout bug: dragging a divider on desktop sets an inline pixel `flex-basis`; if the window is then shrunk below the 900px mobile breakpoint (flex-direction switches to column), that same pixel value would misapply as a height instead of a width. Fixed with `flex-basis: auto !important` on #sidebar/#graphPane inside the mobile media query.
- 2026-08-10 — User asked for status sorting; per CLAUDE.md ambiguity rule, asked (AskUserQuestion) what "sort" should target before building, since it materially changes scope (map marker z-order vs. a new sortable table) — user chose a new sortable table with Status/MW/MWh/Datum as priority criteria (Name/Bundesland added too, same mechanism, no extra cost). Added as a second tab ("Liste") inside the existing graph pane rather than a 4th column, to avoid overcrowding the layout. Table respects all active filters, click-to-sort column headers with asc/desc arrow indicators, row click pans the map to that site and opens its popup.
- 2026-08-10 — Caught and fixed an inline-style bug during the table build: `<div style="display:none; ...; display:flex; ...">` had two conflicting `display` declarations in one inline attribute — the later one silently wins, so the table would have been visible by default despite intending it hidden. Moved the flex layout properties to a CSS class, kept only `display` toggled inline by JS.
